document.addEventListener("DOMContentLoaded", function () {
  init();
});

let notes = [];

function init() {
  loadNotes();
  bindEvents();
}

function loadNotes() {
  // Load notes from storage and render them
}

function bindEvents() {
  // Bind events for search input, note buttons, and dropdown menu
}

function searchNotes(query) {
  // Filter and reorder notes based on the search query
}

function copyNoteContent(note) {
  // Copy the note content to the clipboard
}

function editNote(note) {
  // Edit note title and content
}

function removeNote(note) {
  // Remove the note from storage and DOM
}

function saveNote(note) {
  // Save the note to storage
}